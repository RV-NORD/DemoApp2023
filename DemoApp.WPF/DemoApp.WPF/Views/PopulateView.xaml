<UserControl x:Class="DemoApp.WPF.Views.PopulateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DemoApp.WPF.Views"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:DemoApp.WPF.ViewModels"
             d:DataContext="{d:DesignInstance vm:PopulateViewModel, IsDesignTimeCreatable=True}"
             xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="200">
            <Button Content="СТАРТ" Command="{Binding StartCommand}"/>
            <Button Content="СТОП" Command="{Binding StopCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
